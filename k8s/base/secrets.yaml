# This is a TEMPLATE file - DO NOT commit actual secrets to Git
# Use kubectl create secret or sealed-secrets/external-secrets in production
#
# To create secrets from .env file:
# kubectl create secret generic ereuna-secrets --from-env-file=.env -n ereuna-prod
#
# Or create manually:
# kubectl create secret generic ereuna-secrets \
#   --from-literal=STRIPE_SECRET_KEY='your-key' \
#   --from-literal=VITE_STRIPE_PUBLISHABLE_KEY='your-key' \
#   --from-literal=MONGODB_URI='mongodb://mongodb:27017/EreunaDB' \
#   --from-literal=TIINGO_KEY='your-key' \
#   --from-literal=VITE_EREUNA_KEY='your-key' \
#   --from-literal=CF_DNS_API_TOKEN='your-token' \
#   --from-literal=CLOUDFLARE_EMAIL='your-email' \
#   --from-literal=GF_SECURITY_ADMIN_PASSWORD='your-password' \
#   -n ereuna-prod

apiVersion: v1
kind: Secret
metadata:
  name: ereuna-secrets
  namespace: ereuna-prod
type: Opaque
stringData:
  # REPLACE THESE WITH YOUR ACTUAL VALUES OR USE kubectl create secret
  STRIPE_SECRET_KEY: "REPLACE_ME"
  VITE_STRIPE_PUBLISHABLE_KEY: "REPLACE_ME"
  MONGODB_URI: "mongodb://mongodb:27017/EreunaDB"
  TIINGO_KEY: "REPLACE_ME"
  VITE_EREUNA_KEY: "REPLACE_ME"
  CF_DNS_API_TOKEN: "REPLACE_ME"
  CLOUDFLARE_EMAIL: "REPLACE_ME"
  GF_SECURITY_ADMIN_PASSWORD: "REPLACE_ME"
  JWT_SECRET: "REPLACE_ME"
---
apiVersion: v1
kind: Secret
metadata:
  name: grafana-secrets
  namespace: ereuna-monitoring
type: Opaque
stringData:
  admin-user: "admin"
  admin-password: "REPLACE_ME"
