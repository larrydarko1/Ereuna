# Every route in this application is behind authentication; the three public
# ones (login, signup, recovery) are sign-in forms with nothing to index.
# There is no crawlable surface, so nothing is allowed.
User-agent: *
Disallow: /
