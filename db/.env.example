# MongoDB connection for the migrate-mongo CLI.
# The API, worker and ingestor read their own copies from their own .env files —
# this one is only ever used by `npm run db:migrate`.
MONGO_URI=mongodb://localhost:27017
MONGO_DB=EreunaDB
